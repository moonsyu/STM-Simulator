# 현재 검증 기록 — 0.8.0

2026-09-07, Windows x64, Node.js 24.18.0, Electron 44.2.0, Playwright 1.58.2.

## 변경

- HAL 예제를 선택하면 확인 창 없이 회로·배선·코드·핀·계산 설정을 즉시 적용한다. 회로 이름을 유지하고 Undo/Redo로 이전 작업을 복원한다.
- 16종 부품을 검색 가능한 하나의 목록으로 통합했다. 한글·영문 이름·종류·설명으로 검색하며 빈 문자열과 공백만 입력하면 모두 표시한다.
- 부품 목록만 세로 스크롤하며 검색창과 예제·핀 설정은 제자리에 유지한다. 결과가 없으면 안내하고 실행 중에는 부품 추가를 막는다.
- README를 프로젝트 소개·기능·빌드 방법·GitHub Releases 다운로드 안내로 갱신했다.

## 검증 상태

- npm test: **111개 통과**, 실패 없음. 회로·HAL·UART·인터럽트·파일 호환 및 빌드 정리 검증을 포함한다.
- 부품 및 HAL 예제 UI 개별 검사 통과. 검색 초기화/별칭/결과 없음, 최소 창 1120×760에서 목록 스크롤, 필터된 부품 추가, 예제 즉시 적용과 Undo/Redo·이름 보존을 검사했다.
- npm run test:ui: **7개 스크립트 통과**, 렌더러 오류 없음. 예제 선택 직후 Ctrl+Z 포커스 수정을 적용한 뒤 HAL 예제 UI를 다시 검사해 전체 회로 복원을 확인했다.
- 배포 EXE 검증 결과와 게시 정보는 배포 완료 후 이 문서에 갱신한다.

## 범위와 증거

지원 HAL API를 회로에 연결하는 소스 인터프리터이며 전체 ST 드라이버·ARM/ELF/BIN 실행기는 아니다. HAL DMA, 동기식 USART, MCU UART 간 직접 배선 전송, 실제 NVIC 선점은 미지원이다.

소스는 C:/Users/SSAFY/Desktop/ct/STM-Simulator, 최신 배포 묶음은 dist/artifact/, 현재 실행 증거는 test-results/에 유지한다. 테스트는 사용자 자동 저장과 분리한 프로필을 사용하며 완료 후 임시 프로필과 중복 생성물을 정리한다.
